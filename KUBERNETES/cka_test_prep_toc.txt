CKA TEST PREPARATION — TABLE OF CONTENTS (AUGMENTED)
==================================================
File: cka_test_prep.html
TOC File: cka_test_prep_toc.txt
Purpose: Comprehensive CKA exam test preparation — Pass the CKA & Beyond
Based on: Official Linux Foundation CKA Exam Domains (5 categories)
Project: anihpj/jobpost (Django job posting application on AWS)
Target Audience: Novice → Professional → Beyond (all levels)
    • Novices: Foundational concepts, analogies, first-principles explanations
    • Intermediate: Deep dives, troubleshooting patterns, production scenarios
    • Advanced/Professional: Architecture decisions, HA patterns, multi-cluster, operators
    • Beyond CKA: CKAD crossover, CKS security patterns, real-world DevOps workflows
Style: Highly illustrative — ASCII diagrams, tables, cards, decision trees, flowcharts
Reference: Modeled after cilium-test-prep.html design patterns


═══════════════════════════════════════════════════════════════════════
PRE-LEARNING: KUBERNETES FOUNDATIONS (Novice Foundation)
═══════════════════════════════════════════════════════════════════════
Before diving into the exam domains, this section builds your mental model
of what Kubernetes IS, why it exists, and how everything fits together.
Essential for novices; useful refresher for experienced engineers.

F.1 What Problem Does Kubernetes Solve?
    • The "It Works on My Machine" Problem
    • From Bare Metal → VMs → Containers → Orchestration
    • Monolith vs Microservices — why anihpj uses Django + PostgreSQL + Redis
    • The 12-Factor App methodology in practice

F.2 Containers 101 — Docker & OCI
    • What IS a container? (Process isolation, not a "mini VM")
    • Dockerfile anatomy (FROM, WORKDIR, COPY, RUN, CMD, EXPOSE)
    • Image layers and caching — why COPY requirements.txt before COPY .
    • Docker build/push/pull/run lifecycle
    • Container registries (Docker Hub, ECR, Harbor)
    🟢 Novice | 🟡 Intermediate: Multi-stage builds, distroless images, layer optimization

F.3 Kubernetes Core Concepts — The "Big Picture" Mental Model
    • Cluster = Control Plane + Worker Nodes (ASCII diagram)
    • Pod = smallest deployable unit (1+ containers sharing network namespace)
    • Declarative vs Imperative: "I want 3 replicas" vs "start 3 pods"
    • Desired State vs Actual State — the reconciliation loop
    • kubectl: your remote control for the cluster
    🟢 Novice | 🟡 Intermediate: Controller pattern deep dive, custom controllers

F.4 The anihpj/jobpost Application Stack
    • Web tier (Django templates + static files) — anihpj-web
    • API tier (Django REST Framework) — anihpj-api
    • Database tier (PostgreSQL 15) — anihpj-db
    • Redis (Celery task queue + caching) — anihpj-redis
    • How these tiers talk to each other (Service discovery via DNS)
    • Complete file tree diagram of the Django project

F.5 Kubernetes vs Docker Compose — A Mental Bridge
    • docker-compose.yml → Kubernetes YAML mapping table
    • Why Kubernetes when Docker Compose works fine on one machine?
    • The "Day 2" problem: self-healing, scaling, rolling updates


═══════════════════════════════════════════════════════════════════════
EXAM DOMAIN DISTRIBUTION (Linux Foundation Official)
═══════════════════════════════════════════════════════════════════════

  #  Category                                     Weight    Part 1    Part 2    Part 3
  ──  ──────────────────────────────────────────  ──────    ──────    ──────    ──────
  1   Storage                                       10%       20 Qs     10 Iss    10 Scn
  2   Troubleshooting                               30%       60 Qs     30 Iss    30 Scn
  3   Workloads & Scheduling                        15%       30 Qs     15 Iss    15 Scn
  4   Cluster Architecture, Installation & Config   25%       50 Qs     25 Iss    25 Scn
  5   Services & Networking                         20%       40 Qs     20 Iss    20 Scn
  ──  ──────────────────────────────────────────  ──────    ──────    ──────    ──────
  TOTALS                                           100%      200        100       100


═══════════════════════════════════════════════════════════════════════
PART 1: 200 MULTIPLE CHOICE QUESTIONS (MCQs)
═══════════════════════════════════════════════════════════════════════
Organized by the 5 official Linux Foundation CKA categories
Each question includes: Answer key + Detailed explanation in plain English
Novice-friendly: Every concept explained from first principles with analogies

───────────────────────────────────────────────────────────────────────
CATEGORY 1: STORAGE (10% = 20 Questions) — Q1-Q20
───────────────────────────────────────────────────────────────────────
"This category is about where your application's data lives and how to
make it survive pod restarts. Think of it like deciding between a
whiteboard (temporary/emptyDir), a filing cabinet (persistent volume),
or a cloud drive (CSI)."

1.1 Understanding Storage Fundamentals (4 Qs)
    Q1:  What happens to data when a pod restarts? (emptyDir vs PV)
    Q2:  Container filesystem layers — read-only image + writable layer
    Q3:  Volumes vs VolumeMounts — the "plug and socket" analogy
    Q4:  Why databases need PersistentVolumes (StatefulSet vs Deployment)

1.2 Storage Classes & Dynamic Provisioning (6 Qs)
    Q5:  What is a StorageClass? (The "menu" for storage types)
    Q6:  Provisioners — AWS EBS, GCE PD, NFS, hostPath, local
    Q7:  reclaimPolicy: Retain vs Delete vs Recycle (when PVC is deleted)
    Q8:  volumeBindingMode: Immediate vs WaitForFirstConsumer
    Q9:  allowVolumeExpansion — can you grow a volume after creation?
    Q10: Setting a default StorageClass and why it matters

1.3 Persistent Volumes & Persistent Volume Claims (6 Qs)
    Q11: PV vs PVC — the "parking spot" vs "parking reservation" analogy
    Q12: accessModes: RWO (ReadWriteOnce), ROX (ReadOnlyMany), RWX (ReadWriteMany)
    Q13: What happens when no PV matches a PVC? (Pending state debugging)
    Q14: PVC binding to PV — claimRef, pv-protection finalizer, pre-binding
    Q15: Expanding a PVC (resize) — StorageClass must allow it
    Q16: Why a PVC can get "stuck" in Terminating state

1.4 Volume Types Deep Dive (4 Qs)
    Q17: emptyDir — tied to pod lifecycle, good for temp/cache
    Q18: hostPath — mounts node filesystem into pod (security risk!)
    Q19: Projected volumes — combining ConfigMap + Secret + downwardAPI
    Q20: CSI (Container Storage Interface) — pluggable storage drivers


───────────────────────────────────────────────────────────────────────
CATEGORY 2: TROUBLESHOOTING (30% = 60 Questions) — Q21-Q80
───────────────────────────────────────────────────────────────────────
"This is the biggest category and the most practical. Troubleshooting is
30% of the exam because in real life, you spend 30% of your time fixing
things. Learn the systematic approach: Observe → Hypothesize → Test → Fix."

2.1 Cluster & Node Troubleshooting (12 Qs)
    Q21: Node conditions — Ready, MemoryPressure, DiskPressure, PIDPressure, NetworkUnavailable
    Q22: kubectl describe node — reading node conditions and events
    Q23: kubectl cordon → drain → uncordon workflow (safe node maintenance)
    Q24: What happens to pods during kubectl drain? (eviction + PDB)
    Q25: Taints and Tolerations — "this node is reserved for database pods only"
    Q26: kubelet service — systemctl status kubelet, journalctl -u kubelet
    Q27: Node stuck in NotReady — kubelet stopped, network partition, disk full
    Q28: Zombie/Terminating pods — stuck finalizers, force delete
    Q29: Node resource pressure — kubectl top node, kubectl describe node | grep Pressure
    Q30: OOMKilled — understanding OOM score and QoS eviction priority
    Q31: Container runtime issues — containerd/docker not running
    Q32: Node-level logging — /var/log/containers, /var/log/pods

2.2 Control Plane Component Troubleshooting (12 Qs)
    Q33: kube-apiserver — where logs live (/var/log/kube-apiserver.log or journalctl)
    Q34: etcd — raft consensus, leader election, quorum concept
    Q35: kube-controller-manager — what each controller does (node, replication, endpoint, serviceaccount)
    Q36: kube-scheduler — how it picks a node (filtering → scoring → binding)
    Q37: kubelet — node registration, pod admission, CRI interaction
    Q38: kube-proxy — iptables mode vs IPVS mode vs nftables mode
    Q39: Static pod manifests — /etc/kubernetes/manifests/ (kubelet watches this directory!)
    Q40: kubectl get componentstatuses — is the control plane healthy?
    Q41: Certificate expiration — kubeadm certs check-expiration, kubeadm certs renew
    Q42: kubeadm certs — the certificate chain (CA → apiserver → kubelet → etcd)
    Q43: etcd health — etcdctl endpoint health, etcdctl member list
    Q44: API server flags — --advertise-address, --etcd-servers, --service-cluster-ip-range

2.3 Application & Resource Monitoring (12 Qs)
    Q45: kubectl top pod/node — how metrics-server collects data (kubelet Summary API)
    Q46: Resource requests vs limits — "guaranteed" vs "burstable" resources
    Q47: QoS Classes — Guaranteed (request=limit), Burstable, BestEffort
    Q48: How OOM killer decides which pod to kill first (QoS + memory usage)
    Q49: Liveness probe — "is my app alive?" (restart if failing)
    Q50: Readiness probe — "is my app ready to serve?" (remove from Service if failing)
    Q51: Startup probe — "is my app done booting?" (gives slow apps extra time)
    Q52: Probe types — httpGet, tcpSocket, exec (command)
    Q53: Probe parameters — initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold
    Q54: kubectl logs — --previous (crashed container), --tail=N, --since=time, --timestamps
    Q55: Multi-container pod logging — kubectl logs <pod> -c <container>
    Q56: kubectl describe pod — reading Events section for clues

2.4 Container Output Streams & Logging (12 Qs)
    Q57: stdout vs stderr — why "log to stdout, not files" (12-factor app principle)
    Q58: kubectl logs flags deep dive — --follow, --all-containers, --selector
    Q59: Sidecar logging pattern — Fluentd/Fluent Bit as log forwarder sidecar
    Q60: Log rotation — containerLogMaxSize, containerLogMaxFiles in kubelet config
    Q61: kubectl events — cluster-level event log (--sort-by='.lastTimestamp')
    Q62: Audit logs — who did what and when (audit policy, audit backends)
    Q63: Logging architecture — container → kubelet → /var/log/ → log agent → backend
    Q64: Centralized logging with EFK/ELK stack (Elasticsearch + Fluentd + Kibana)
    Q65: kubectl exec — debugging inside a running container
    Q66: kubectl cp — copying files to/from containers
    Q67: kubectl port-forward — accessing pod ports locally for debugging
    Q68: Debugging with ephemeral debug containers (kubectl debug)

2.5 Services & Networking Troubleshooting (12 Qs)
    Q69: DNS troubleshooting — nslookup, dig, /etc/resolv.conf inside pod
    Q70: CoreDNS — ConfigMap (Corefile), plugins (kubernetes, forward, cache, errors)
    Q71: ndots and DNS search domains — why anihpj-api.anihpj.svc.cluster.local works
    Q72: dnsPolicy — ClusterFirst, Default, None, ClusterFirstWithHostNet
    Q73: Service debugging — kubectl get endpoints (must show pod IPs!)
    Q74: Selector mismatch — the #1 cause of "Service not working"
    Q75: kube-proxy modes — iptables (default) vs IPVS (more efficient)
    Q76: NetworkPolicy debugging — kubectl describe networkpolicy
    Q77: CNI plugin troubleshooting — /etc/cni/net.d/, calico/cilium/flannel logs
    Q78: Pod network connectivity checklist — ping, curl, DNS, Service
    Q79: kubectl run test-pod --rm -it --image=busybox — network debug pod
    Q80: iptables rules for Services — iptables -t nat -L KUBE-SERVICES


───────────────────────────────────────────────────────────────────────
CATEGORY 3: WORKLOADS & SCHEDULING (15% = 30 Questions) — Q81-Q110
───────────────────────────────────────────────────────────────────────
"This category is about running your applications: deploying them,
updating them without downtime, configuring them, and making sure they
end up on the right nodes with the right resources."

3.1 Deployments, Rolling Updates & Rollbacks (10 Qs)
    Q81:  Deployment → ReplicaSet → Pod hierarchy (each level adds features)
    Q82:  ReplicaSet — ensures N identical pods are always running
    Q83:  Rolling update strategy — maxSurge (extra pods) + maxUnavailable (missing pods)
    Q84:  What happens during kubectl apply with a new image tag? (step by step)
    Q85:  kubectl rollout status — watching a deployment update
    Q86:  kubectl rollout history — viewing revision history
    Q87:  kubectl rollout undo — rolling back to previous revision
    Q88:  kubectl rollout pause/resume — pausing for controlled rollouts
    Q89:  Blue-Green deployment pattern — two full stacks, switch Service
    Q90:  Canary deployment pattern — gradually shift traffic percentage

3.2 ConfigMaps & Secrets (8 Qs)
    Q91:  ConfigMap — externalize configuration from container images
    Q92:  Creating ConfigMaps — --from-literal, --from-file, --from-env-file
    Q93:  Consuming ConfigMaps — envFrom, valueFrom.configMapKeyRef, volume mount
    Q94:  Updating ConfigMaps — pods DON'T auto-restart (use stakater/Reloader)
    Q95:  Secrets — base64 encoded (NOT encrypted!), Types: Opaque, tls, dockerconfigjson
    Q96:  Creating Secrets — kubectl create secret generic/docker-registry/tls
    Q97:  immutable: true — prevents accidental changes to Secrets/ConfigMaps
    Q98:  Encryption at rest — etcd encryption for Secrets (EncryptionConfiguration)

3.3 Workload Autoscaling (6 Qs)
    Q99:  HPA (Horizontal Pod Autoscaler) — scale pods based on CPU/memory/custom metrics
    Q100: HPA algorithm — desiredReplicas = ceil[currentReplicas * (currentMetric / desiredMetric)]
    Q101: HPA prerequisites — metrics-server, resource requests must be set
    Q102: VPA (Vertical Pod Autoscaler) — adjusts resource requests/limits (restarts pod!)
    Q103: Cluster Autoscaler — adds/removes NODES based on pending pods
    Q104: KEDA (Kubernetes Event-Driven Autoscaling) — scale based on events (Redis queue, Kafka)

3.4 Pod Scheduling, Admission & Self-Healing (6 Qs)
    Q105: nodeSelector — simplest scheduling constraint ("put me on a GPU node")
    Q106: nodeAffinity — requiredDuringScheduling vs preferredDuringScheduling
    Q107: podAffinity/podAntiAffinity — "keep me near/away from other pods"
    Q108: Taints and Tolerations — repelling pods from certain nodes
    Q109: topologySpreadConstraints — spread pods across zones/hosts/regions
    Q110: PriorityClass — which pods get evicted first under resource pressure
    Q110a: ResourceQuota — limit total resources per namespace
    Q110b: LimitRange — set default/min/max resource requests per pod/container
    Q110c: PodDisruptionBudget (PDB) — minimum pods that must stay running during eviction
    Q110d: restartPolicy — Always (default), OnFailure (batch jobs), Never (init/oneshot)


───────────────────────────────────────────────────────────────────────
CATEGORY 4: CLUSTER ARCHITECTURE (25% = 50 Questions) — Q111-Q160
───────────────────────────────────────────────────────────────────────
"This is the 'build and run the cluster' category. You need to understand
every component, how to bootstrap a cluster with kubeadm, how to upgrade
it, how to secure it with RBAC, and how to extend it with operators."

4.1 Kubernetes Architecture Deep Dive (10 Qs)
    Q111: Control plane components — API server, etcd, scheduler, controller-manager
    Q112: API server — the "front desk" of Kubernetes (REST API, authentication, admission)
    Q113: etcd — distributed key-value store (RAFT consensus, quorum = N/2+1)
    Q114: kube-scheduler — filtering (predicates) → scoring (priorities) → binding
    Q115: kube-controller-manager — the reconciliation engine (node, replication, endpoint, SA)
    Q116: cloud-controller-manager — cloud-specific controllers (AWS, GCP, Azure)
    Q117: Worker node components — kubelet, kube-proxy, container runtime (containerd/CRI-O)
    Q118: CRI (Container Runtime Interface) — why Docker was replaced by containerd
    Q119: The reconciliation loop — observe → diff → act (the heart of Kubernetes)
    Q120: Request flow: kubectl → API server → Authentication → Authorization → Admission → etcd

4.2 kubeadm Cluster Bootstrapping (10 Qs)
    Q121: kubeadm init — what happens step by step (preflight → certs → kubeconfig → manifests → etcd)
    Q122: kubeadm initConfiguration and clusterConfiguration YAML
    Q123: --pod-network-cidr — why CNI plugins need this (Calico: 192.168.0.0/16, Flannel: 10.244.0.0/16)
    Q124: kubeadm join — how worker nodes join (token + discovery-token-ca-cert-hash)
    Q125: kubeadm token — create, list, delete (tokens expire in 24h by default)
    Q126: kubeadm certs check-expiration — when do certificates expire?
    Q127: kubeadm certs renew — renew all certificates before they expire
    Q128: kubeadm upgrade plan → kubeadm upgrade apply (step by step)
    Q129: kubeadm reset — full cleanup (stop kubelet, remove containers, clean etcd)
    Q130: kubelet configuration — /var/lib/kubelet/config.yaml, kubeadm-config ConfigMap

4.3 High Availability & Lifecycle Management (10 Qs)
    Q131: HA control plane — stacked etcd (3 control planes, each with etcd) vs external etcd
    Q132: Load balancer for API server — HAProxy, nginx, or cloud LB (TCP port 6443)
    Q133: etcd quorum — why you need 3 or 5 nodes (odd number, N/2+1 for quorum)
    Q134: etcd backup — etcdctl snapshot save (point-in-time backup)
    Q135: etcd restore — etcdctl snapshot restore (full cluster recovery)
    Q136: Kubernetes version skew policy — kube-apiserver can be ±1 minor version from kubelet
    Q137: Cluster upgrade process — control plane first, then workers, then kubelet
    Q138: Draining nodes before upgrade — kubectl drain --ignore-daemonsets --delete-emptydir-data
    Q139: Static pods — kubelet-managed pods from /etc/kubernetes/manifests/ (no controller!)
    Q140: Multi-master cluster — how API servers stay in sync (all write to same etcd)

4.4 RBAC, Security & Access Control (10 Qs)
    Q141: Authentication vs Authorization vs Admission Control (3 A's of security)
    Q142: Role (namespace-scoped) vs ClusterRole (cluster-scoped)
    Q143: RoleBinding vs ClusterRoleBinding — binding subjects to roles
    Q144: Verbs — get, list, watch, create, update, patch, delete, deletecollection
    Q145: ServiceAccounts — identity for pods (each namespace has a "default" SA)
    Q146: automountServiceAccountToken — when you DON'T want a token in your pod
    Q147: kubeconfig file structure — clusters: users: contexts: (triple mapping)
    Q148: kubectl config — use-context, set-credentials, set-cluster, view
    Q149: Certificate Signing Requests (CSR) — kubectl certificate approve/deny
    Q150: Node authorization — nodes can only read their own secrets and pods

4.5 Helm, Kustomize & Cluster Extensibility (10 Qs)
    Q151: Helm v3 architecture — client-only (no Tiller!), chart repository
    Q152: Helm chart structure — Chart.yaml, values.yaml, templates/, charts/
    Q153: helm install/upgrade/rollback/uninstall — release lifecycle
    Q154: Helm values — --set, --values, --reuse-values, default values.yaml
    Q155: Kustomize — declarative configuration without templates
    Q156: Kustomize overlays — dev/staging/prod using bases + patches + overlays
    Q157: kustomization.yaml — resources, patchesStrategicMerge, patchesJson6902, images
    Q158: CRDs (Custom Resource Definitions) — extending the Kubernetes API
    Q159: Operators — CRD + controller = operator (automated application management)
    Q160: CNI (Container Network Interface), CSI (Container Storage Interface), CRI (Container Runtime Interface)


───────────────────────────────────────────────────────────────────────
CATEGORY 5: SERVICES & NETWORKING (20% = 40 Questions) — Q161-Q200
───────────────────────────────────────────────────────────────────────
"Networking is the nervous system of Kubernetes. Every pod gets its own
IP, services provide stable endpoints, and network policies are the
firewall. If you can't reach your app, this category has the answers."

5.1 Pod Networking Fundamentals (10 Qs)
    Q161: The Kubernetes networking model — every pod gets a unique, routable IP
    Q162: "IP-per-pod" — why this simplifies everything (no port conflicts!)
    Q163: CNI (Container Network Interface) — ADD/DEL/VERSION operations
    Q164: Overlay networks — VXLAN (Flannel), IP-in-IP (Calico), how they work
    Q165: Underlay/BGP networks — Calico with BGP, direct routing (no encapsulation)
    Q166: kube-proxy modes — iptables (sequential rules) vs IPVS (hash table, faster)
    Q167: Pod-to-pod communication on same node (veth pair + bridge/cni0)
    Q168: Pod-to-pod communication across nodes (overlay tunnel or direct routing)
    Q169: Pod-to-Service communication — kube-proxy rewrites destination IP:Port
    Q170: eBPF-based networking — Cilium replaces kube-proxy with BPF programs

5.2 Service Types & Endpoints Deep Dive (10 Qs)
    Q171: ClusterIP — internal-only, virtual IP (kube-proxy load balances)
    Q172: NodePort — opens a port (30000-32767) on EVERY node (not just the pod's node!)
    Q173: LoadBalancer — cloud LB (AWS NLB, GCP TCP LB) → NodePort → pod
    Q174: ExternalName — CNAME alias for external services (RDS, ElastiCache)
    Q175: Headless Service (clusterIP: None) — DNS returns pod IPs directly
    Q176: Endpoints vs EndpointSlices — scaling endpoint updates for large clusters
    Q177: service topology — topologyKeys for zone-aware routing
    Q178: externalTrafficPolicy — Cluster (source NAT, load balanced) vs Local (preserve source IP)
    Q179: sessionAffinity — ClientIP sticky sessions (not recommended for stateless apps)
    Q180: Service without selector — manually managed endpoints for external services

5.3 Network Policies — The Kubernetes Firewall (10 Qs)
    Q181: Ingress vs Egress — which direction does traffic flow?
    Q182: podSelector — which pods does this policy apply to?
    Q183: namespaceSelector — allow traffic from specific namespaces
    Q184: ipBlock — CIDR-based rules (allow office VPN IPs)
    Q185: Default-deny all ingress — secure by default (deny all, then add allows)
    Q186: Default-deny all egress — restrict outbound traffic (allow only DNS + API)
    Q187: Policy ordering — policies are ADDITIVE (multiple policies combine, no priority)
    Q188: CNI requirement — NetworkPolicy ONLY works if your CNI supports it
    Q189: kubectl describe networkpolicy — debugging policy effects
    Q190: Real-world policy example — allow anihpj-web → anihpj-api:8000 only

5.4 Ingress, Gateway API & TLS (10 Qs)
    Q191: Ingress — L7 (HTTP/HTTPS) routing (host + path → backend Service)
    Q192: Ingress controller — nginx-ingress, traefik, AWS ALB Ingress Controller
    Q193: IngressClass — which controller handles this Ingress? (default vs specific)
    Q194: Path types — Prefix (/api matches /api/v1/jobs), Exact, ImplementationSpecific
    Q195: TLS termination — TLS secret + hosts in Ingress spec
    Q196: Ingress annotations — configuration knobs for specific controllers
    Q197: Gateway API — next-gen Ingress (GatewayClass, Gateway, HTTPRoute)
    Q198: HTTPRoute — path matching, header matching, traffic splitting (canary!)
    Q199: cert-manager + Let's Encrypt — automatic TLS certificate provisioning
    Q200: Debugging Ingress — 502 Bad Gateway, 503 Service Unavailable, 504 Gateway Timeout


═══════════════════════════════════════════════════════════════════════
PART 2: 100 TROUBLESHOOTING ISSUES
═══════════════════════════════════════════════════════════════════════
Each issue follows the cilium-test-prep.html format:
  5 Most Likely Causes (🔴 — common in production)
  5 Less Likely Causes (🟡 — edge cases, rare scenarios)
  5 New Cluster Causes (🟣 — specific to freshly bootstrapped clusters)
  🔍 What to Look At / Take Note Of
  🔧 How to Solve (numbered steps with commands)
  💡 Personal Advice & Recommendations (from real-world experience)
  📋 Decision tree checklist (flowchart for systematic debugging)

───────────────────────────────────────────────────────────────────────
CATEGORY 1: STORAGE (10 Issues: ST1-ST10)
───────────────────────────────────────────────────────────────────────

  ST1: PVC Stuck in Pending — No StorageClass
       Symptom: kubectl get pvc shows Pending forever. No PV provisioned.
       anihpj: PostgreSQL PVC for anihpj-db won't bind

  ST2: PVC Access Mode Mismatch — RWO vs RWX
       Symptom: PV exists but PVC won't bind. accessModes don't match.
       anihpj: Two API pods on different nodes need same volume (RWX required)

  ST3: Volume Not Expanding — allowVolumeExpansion Not Set
       Symptom: Edited PVC to increase size, but it stays at old capacity.
       anihpj: Database growing, need to expand from 5Gi to 20Gi

  ST4: CSI Driver Not Found — Dynamic Provisioning Fails
       Symptom: StorageClass references a provisioner that doesn't exist.
       anihpj: Using AWS EBS CSI driver but not installed

  ST5: PV in Released State — Can't Reuse
       Symptom: Deleted PVC, but PV shows "Released" not "Available"
       anihpj: Cleaning up test namespace, need to reuse the EBS volume

  ST6: Pod Can't Mount Volume — Permission Denied
       Symptom: Pod stuck in ContainerCreating, events show "permission denied"
       anihpj: fsGroup not set, PostgreSQL can't write to data directory

  ST7: DiskPressure — Node Evicting Pods
       Symptom: Node shows DiskPressure=True, pods being evicted
       anihpj: Container logs filling up /var on worker node

  ST8: hostPath Volume — Data Lost After Pod Reschedule
       Symptom: Pod restarted on different node, data missing
       anihpj: Used hostPath for database storage (wrong choice!)

  ST9: ConfigMap Too Large — Can't Mount as Volume
       Symptom: ConfigMap exceeds 1MB etcd limit
       anihpj: Large nginx config file as ConfigMap

  ST10: Volume SubPath Not Updating
        Symptom: Updated ConfigMap, but subPath mount still shows old content
        anihpj: Mounted specific config file via subPath, changes not reflected


───────────────────────────────────────────────────────────────────────
CATEGORY 2: TROUBLESHOOTING (30 Issues: TR1-TR30)
───────────────────────────────────────────────────────────────────────

  TR1:  Node NotReady — kubelet Stopped
  TR2:  Node MemoryPressure — OOM Killer Evicting Pods
  TR3:  Node DiskPressure — Image GC and Container Logs
  TR4:  Node PIDPressure — Too Many Processes
  TR5:  Node NetworkUnavailable — CNI Not Ready
  TR6:  API Server Unreachable — kubectl timeout
  TR7:  etcd Leader Election Storm — Frequent Leader Changes
  TR8:  kube-scheduler Not Scheduling Pods
  TR9:  kube-controller-manager Not Reconciling
  TR10: Static Pod Not Restarting — Manifest Error
  TR11: ImagePullBackOff — Wrong Image Tag or Registry Auth
  TR12: ErrImagePull — Private Registry Credentials Missing
  TR13: CrashLoopBackOff — Application Exits Immediately
  TR14: CreateContainerError — Invalid Command or Args
  TR15: RunContainerError — Container Runtime Failure
  TR16: OOMKilled — Container Exceeded Memory Limit
  TR17: CPU Throttling — Application Slow Under Load
  TR18: Init Container Failure — Dependency Not Ready
  TR19: postStart Hook Failure — Container Killed After Start
  TR20: Pod Stuck Terminating — Finalizer Not Removed
  TR21: Liveness Probe Too Aggressive — Healthy Pod Restarted
  TR22: Readiness Probe Never Succeeds — Service No Endpoints
  TR23: Startup Probe Timeout — Slow-Starting App Killed
  TR24: Exec Probe Permission Denied — No Shell in Container
  TR25: HTTP Probe 404 — Wrong Health Check Path
  TR26: kubectl logs Returns Nothing — Logging to Files, Not stdout
  TR27: Disk Full from Container Logs — Log Rotation Misconfigured
  TR28: kubectl exec Fails — pts/0 Allocation Error
  TR29: metrics-server Not Reporting — kubectl top Returns Error
  TR30: Event Rate Limiting — kubectl get events Shows Gaps


───────────────────────────────────────────────────────────────────────
CATEGORY 3: WORKLOADS & SCHEDULING (15 Issues: WS1-WS15)
───────────────────────────────────────────────────────────────────────

  WS1:  Deployment Rollout Stuck — ReplicaSet Not Progressing
  WS2:  kubectl rollout undo — Wrong Revision Restored
  WS3:  Deployment Paused — Forgotten kubectl rollout pause
  WS4:  Pod Stuck Pending — No Suitable Node (All Resources Exhausted)
  WS5:  nodeSelector Mismatch — Pod Won't Schedule to Expected Node
  WS6:  Pod Anti-Affinity Too Strict — Can't Place Pods
  WS7:  ConfigMap Update Not Reflected — Pod Still Using Old Config
  WS8:  Secret base64 Corruption — Decode/Encode Errors
  WS9:  immutable ConfigMap — Can't Update Without Recreate
  WS10: HPA Not Scaling — metrics-server Missing or Resource Requests Not Set
  WS11: HPA Scaling Too Aggressively — Flapping Replicas
  WS12: VPA + HPA Conflict — Both Trying to Control Replicas
  WS13: PodDisruptionBudget Blocks Node Drain — Too Strict minAvailable
  WS14: QoS Class Unexpected — Request ≠ Limit Changes Behavior
  WS15: Preemption — Lower Priority Pod Evicted Unexpectedly


───────────────────────────────────────────────────────────────────────
CATEGORY 4: CLUSTER ARCHITECTURE (25 Issues: CA1-CA25)
───────────────────────────────────────────────────────────────────────

  CA1:  kubeadm init Fails — Preflight Check Errors
  CA2:  kubeadm join Token Expired — Worker Node Can't Join
  CA3:  Certificate Expired — kubectl Returns x509 Error
  CA4:  kubeadm upgrade Blocked — Version Skew Too Large
  CA5:  kubelet Config Drift — Node Behaves Differently
  CA6:  etcd Quorum Lost — Cluster Unavailable
  CA7:  etcd High Disk Latency — Leader Elections Frequent
  CA8:  etcd Database Corruption — Checksum Mismatch
  CA9:  etcd Backup — Snapshot Saves Cluster State
  CA10: etcd Member Add/Remove — Wrong Peer URLs
  CA11: API Server Load Balancer Misconfigured — Connection Refused
  CA12: API Server High Latency — etcd Slow or Too Many Requests
  CA13: Stacked etcd vs External etcd — Pros and Cons
  CA14: kubelet Fails to Register Node — Hostname Mismatch
  CA15: Multiple API Servers — Stale Data from Cache
  CA16: kubectl Forbidden — Missing RBAC Role/Binding
  CA17: ServiceAccount Token Not Auto-Mounted — Pod Can't Auth
  CA18: ClusterRoleBinding to Wrong Subject — User Still Forbidden
  CA19: CSR Pending Forever — Manual Approval Required
  CA20: kubeconfig Context Wrong — kubectl Talking to Wrong Cluster
  CA21: CRD Schema Validation Error — Custom Resource Rejected
  CA22: Operator Not Reconciling — Controller Loop Broken
  CA23: Admission Webhook Timeout — Pod Creation Stuck
  CA24: Helm Release in Failed State — Can't Upgrade or Uninstall
  CA25: Kustomize Build Error — Patch Target Not Found


───────────────────────────────────────────────────────────────────────
CATEGORY 5: SERVICES & NETWORKING (20 Issues: SN1-SN20)
───────────────────────────────────────────────────────────────────────

  SN1:  Service Has No Endpoints — Selector Not Matching Pods
  SN2:  ClusterIP Not Reachable from Pod — kube-proxy Issue
  SN3:  NodePort Not Accessible Externally — Firewall/Security Group
  SN4:  LoadBalancer Stuck Pending — Cloud Controller Not Running
  SN5:  Headless Service — Why Is Pod DNS Returning No Records?
  SN6:  CoreDNS CrashLoopBackOff — ConfigMap Corruption
  SN7:  Pod DNS Resolution Fails — ndots=5 and Search Domain Cascading
  SN8:  CoreDNS Cache Serving Stale Records — High TTL
  SN9:  Custom DNS (Stub Domain) Not Working
  SN10: Service Discovery via SRV Records Not Working
  SN11: NetworkPolicy Blocks Legitimate Traffic — Overly Restrictive
  SN12: NetworkPolicy Not Applied — CNI Doesn't Support NetworkPolicy
  SN13: Default-Deny Locks You Out — Can't Reach API Server
  SN14: Ingress vs Egress Direction Confusion — Policy Reversed
  SN15: ipBlock CIDR Overlap — Policy Matches Unexpected Traffic
  SN16: Ingress Returns 502 Bad Gateway — Backend Pod Down
  SN17: Ingress TLS Certificate Error — Secret Not Found or Wrong Format
  SN18: IngressClass Not Set — Multiple Controllers, No Default
  SN19: Path-Based Routing — /api Goes to Wrong Backend
  SN20: cert-manager Not Issuing Certificate — Challenge Fails


═══════════════════════════════════════════════════════════════════════
PART 3: 100 HANDS-ON LAB SCENARIOS (S1-S100)
═══════════════════════════════════════════════════════════════════════
Each scenario follows the cilium-test-prep.html design:
  📋 Deploy — YAML with a BUG you must find and fix
  🔍 Investigate — commands to diagnose the problem
  🐛 The Bug — what's wrong and why it happens (spoiler: hidden until you click)
  🔧 Fix — copy-paste commands to resolve the issue
  ✅ Verify — commands to confirm the fix worked
  🧹 Cleanup — delete resources created during the lab
  📋 Copy button on ALL code blocks for direct kubeadm execution

───────────────────────────────────────────────────────────────────────
CATEGORY 1: STORAGE (10 Scenarios: S1-S10)
───────────────────────────────────────────────────────────────────────

  S1:  Deploy PostgreSQL for anihpj — Debug Pending PVC
       Create a PV/PVC for the jobpost database. The PVC stays Pending.
       Bug: StorageClass name typo. Learn: dynamic provisioning flow.

  S2:  RWO vs RWX — Two API Pods, One Volume
       Deploy two anihpj-api pods. Only one can mount the shared volume.
       Bug: EBS only supports RWO, need EFS for RWX. Learn: accessModes.

  S3:  Expand PVC for Growing Database
       Jobpost DB running low on space. Resize the PVC from 5Gi to 20Gi.
       Bug: StorageClass has allowVolumeExpansion: false. Learn: volume expansion.

  S4:  ConfigMap as Volume — Mount nginx Config
       Mount a custom nginx config into the anihpj-web pod.
       Bug: subPath used, changes to ConfigMap not reflected. Learn: subPath behavior.

  S5:  emptyDir vs Persistent — Data After Pod Restart
       Deploy Redis on emptyDir. Kill the pod. Data is gone.
       Bug: emptyDir is ephemeral. Learn: when to use each volume type.

  S6:  Secret as Environment Variable — Database Password
       Inject PostgreSQL password via Secret into anihpj-api pods.
       Bug: Secret referenced but not created. Learn: Secret consumption patterns.

  S7:  CSI Driver Missing — EBS Provisioning Fails
       Deploy anihpj with StorageClass using ebs.csi.aws.com provisioner.
       Bug: AWS EBS CSI driver not installed. Learn: CSI architecture.

  S8:  Volume Mount Permission Denied — fsGroup
       Pod can't write to mounted PV. Events show "permission denied".
       Bug: Container runs as non-root, volume owned by root. Learn: fsGroup + securityContext.

  S9:  hostPath Volume — Pod Rescheduled, Data Lost
       Pod uses hostPath to store data. Pod evicted to another node.
       Bug: hostPath data is node-specific. Learn: hostPath limitations.

  S10: ResourceQuota — PVC Limit Exceeded
        Namespace has a ResourceQuota limiting PVC count to 2.
        Bug: Attempt to create 3rd PVC fails. Learn: ResourceQuota enforcement.


───────────────────────────────────────────────────────────────────────
CATEGORY 2: TROUBLESHOOTING (30 Scenarios: S11-S40)
───────────────────────────────────────────────────────────────────────

  S11: Node NotReady — Debug kubelet (systemctl, journalctl)
  S12: MemoryPressure — Evict Pods & Troubleshoot OOM
  S13: DiskPressure — Clean Images, Logs, and EmptyDirs
  S14: etcd Quorum Lost — Snapshot & Disaster Recovery
  S15: API Server Unreachable — Network Debug with tcpdump
  S16: ImagePullBackOff — Private Registry Auth with imagePullSecrets
  S17: CrashLoopBackOff — anihpj-api Missing Database Connection
  S18: OOMKilled — Set Memory Limits for Java/Django App
  S19: Init Container Failure — Wait for Database to Be Ready
  S20: Pod Stuck Terminating — Remove Finalizer Blocking Deletion
  S21: Liveness Probe Misconfigured — Premature Pod Kill on Startup
  S22: Readiness Probe Failing — anihpj-api /health Returns 500
  S23: Startup Probe — Give anihpj-api 120s to Boot Before Liveness
  S24: Exec Probe — livenessProbe with Wrong Shell Command
  S25: HTTP Probe Wrong Path — /healthz vs /health
  S26: Missing Container Logs — anihpj Writing to File, Not stdout
  S27: metrics-server Not Reporting — Debug kubelet Summary API
  S28: kubectl exec Timeout — Debug API Server to Kubelet Connection
  S29: Event Rate Limiting — Missing Events During Pod Failure
  S30: Audit Log — Track Who Deleted the anihpj Namespace


───────────────────────────────────────────────────────────────────────
CATEGORY 3: WORKLOADS & SCHEDULING (15 Scenarios: S41-S55)
───────────────────────────────────────────────────────────────────────

  S41: Rolling Update Stuck — maxSurge=0 + maxUnavailable=0
  S42: Rollback to Previous Revision — kubectl rollout undo
  S43: Blue-Green Deployment — Switch anihpj-web Service Between Stacks
  S44: Canary Release — 10% Traffic to anihpj-api v2
  S45: Deployment Paused — Resume & Debug Rollout Status
  S46: Pod Stuck Pending — All Nodes Have Insufficient CPU
  S47: nodeSelector — anihpj-api Must Run on SSD Nodes Only
  S48: Pod Anti-Affinity — Spread anihpj-web Across AWS AZs
  S49: Taint/Toleration — Dedicate a Node for anihpj-db
  S50: Topology Spread — Distribute anihpj-api Evenly Across Zones
  S51: ConfigMap Hot-Reload — Rolling Restart After Config Change
  S52: Secret Rotation — Update DB Password Without Downtime
  S53: HPA — Auto-Scale anihpj-api Based on CPU (Target 50%)
  S54: PodDisruptionBudget — Ensure at Least 2 API Pods During Drain
  S55: Resource Quota — Prevent anihpj-dev From Consuming All Cluster CPU


───────────────────────────────────────────────────────────────────────
CATEGORY 4: CLUSTER ARCHITECTURE (25 Scenarios: S56-S80)
───────────────────────────────────────────────────────────────────────

  S56: kubeadm init — Bootstrap a 3-Node Cluster from Scratch
  S57: kubeadm join — Worker Node Token Expired
  S58: kubeadm upgrade — v1.28 → v1.29 Step by Step
  S59: Certificate Expiration — Renew All Certificates
  S60: kubeadm reset — Tear Down and Rebuild a Node
  S61: etcd Backup — Snapshot Save to S3
  S62: etcd Restore — Full Disaster Recovery from Snapshot
  S63: etcd Defrag — Reclaim Disk Space After Compaction
  S64: etcd Member Add — Scale from 3 to 5 Members
  S65: etcd Health Check — Detect Slow Disk Before Failure
  S66: Create Read-Only Developer — RBAC for anihpj-dev Namespace
  S67: ServiceAccount + Role — Pod-Level Permissions for CI/CD
  S68: Debug Forbidden — Missing ClusterRoleBinding for Dashboard
  S69: CSR Approval — Node Bootstrap Certificate
  S70: Multi-Cluster kubeconfig — Manage Dev and Prod from One Terminal
  S71: Helm Install anihpj — Full Chart with Custom values.yaml
  S72: Helm Rollback — Failed Upgrade, Recover to Previous Release
  S73: Kustomize Overlay — anihpj Dev/Staging/Prod Environments
  S74: Kustomize Patches — Change Image Tag via Strategic Merge
  S75: Helm + Kustomize — Render Helm Chart, Then Patch with Kustomize
  S76: Create CRD — Define a "JobPost" Custom Resource
  S77: Deploy Operator — Automate anihpj Deployment Lifecycle
  S78: CRD Validation — Fix Schema Error in Custom Resource
  S79: Mutating Webhook — Inject Sidecar into Every Pod
  S80: Install Calico CNI — Replace Default CNI Plugin


───────────────────────────────────────────────────────────────────────
CATEGORY 5: SERVICES & NETWORKING (20 Scenarios: S81-S100)
───────────────────────────────────────────────────────────────────────

  S81: ClusterIP Service — Debug Selector Mismatch (No Endpoints)
  S82: NodePort Service — Expose anihpj-web on Port 30080
  S83: LoadBalancer — AWS NLB for anihpj-web
  S84: Headless Service — StatefulSet with Stable Pod DNS
  S85: ExternalName — Proxy anihpj-api to External RDS
  S86: CoreDNS CrashLoopBackOff — Fix Corefile ConfigMap
  S87: Pod DNS Resolution — ndots:5 Causing 5 DNS Queries per Request
  S88: Custom DNS — Stub Domain for .internal Services
  S89: CoreDNS Cache — Serve Stale on DNS Failure
  S90: SRV Records — Discover anihpj Services via DNS
  S91: Default-Deny NetworkPolicy — Lock Down anihpj Namespace
  S92: Allow Only web→api on Port 8000 — Specific Policy
  S93: Egress to AWS RDS — Allow anihpj-api to Reach External DB
  S94: DNS-Only Egress — Block All Outbound Except CoreDNS
  S95: Debug NetworkPolicy — Traffic Still Flowing After Deny Applied
  S96: NGINX Ingress — Expose anihpj-web via Ingress
  S97: TLS Termination — HTTPS with cert-manager + Let's Encrypt
  S98: Path-Based Routing — /web → anihpj-web, /api → anihpj-api
  S99: Debug 502 Bad Gateway — Ingress Can't Reach Backend
  S100: Gateway API — HTTPRoute with Traffic Splitting (Future-Ready)


═══════════════════════════════════════════════════════════════════════
APPENDICES (A-J)
═══════════════════════════════════════════════════════════════════════

APPENDIX A: COMPLETE ANSWER KEY
    Q1-Q200 correct answers with one-line explanation
    Organized by category for quick reference

APPENDIX B: TOP 50 KUBERNETES COMMANDS BY CATEGORY
    Storage: kubectl get pv/pvc/sc, kubectl describe pv, kubectl patch pvc
    Troubleshooting: kubectl describe, kubectl logs, kubectl exec, kubectl top, journalctl
    Workloads: kubectl create deploy, kubectl rollout, kubectl scale, kubectl autoscale
    Cluster: kubeadm init/join/upgrade/reset, etcdctl, kubectl certs
    Networking: kubectl expose, kubectl get svc/ep, kubectl describe netpol, kubectl ingress

APPENDIX C: ANIHPJ/JOBPOST COMPLETE APPLICATION STRUCTURE
    Full ASCII file tree diagram
    Dockerfile — multi-stage Python 3.11 + Gunicorn
    Dockerfile.dev — development with hot-reload
    docker-compose.yml — local dev (PostgreSQL + Redis + Django)
    requirements.txt — all Python dependencies with versions
    anihpj/settings.py — Django configuration (DB, static, CORS, DRF)
    anihpj/urls.py — root URL routing
    jobpost/models.py — Job, Company, Application Django models
    jobpost/views.py — web and API views
    jobpost/serializers.py — DRF serializers
    jobpost/urls.py — app URL routing

APPENDIX D: COMPLETE KUBERNETES DEPLOYMENT REFERENCE
    anihpj-namespace.yaml — namespace with labels
    anihpj-configmap.yaml — Django settings as ConfigMap
    anihpj-secret.yaml — DB password, secret key, API keys
    anihpj-db-deploy.yaml — PostgreSQL StatefulSet + PVC + Service
    anihpj-api-deploy.yaml — API Deployment (3 replicas) + HPA
    anihpj-web-deploy.yaml — Web Deployment (2 replicas) + HPA
    anihpj-services.yaml — ClusterIP/NodePort/LoadBalancer Services
    anihpj-ingress.yaml — Ingress with TLS termination
    anihpj-networkpolicy.yaml — L3/L4 security policies
    anihpj-pdb.yaml — PodDisruptionBudget for API and Web tiers

APPENDIX E: 15 DECISION TREE FLOWCHARTS
    DT1:  Pod Stuck Pending → Scheduling Decision Tree
    DT2:  CrashLoopBackOff → Application Debug Tree
    DT3:  Service Not Reachable → Network Debug Tree
    DT4:  Node NotReady → Node Health Decision Tree
    DT5:  PVC Stuck Pending → Storage Debug Tree
    DT6:  HPA Not Scaling → Autoscaling Decision Tree
    DT7:  ImagePullBackOff → Registry Debug Tree
    DT8:  kubectl Forbidden → RBAC Authorization Tree
    DT9:  DNS Resolution Failure → CoreDNS Debug Tree
    DT10: x509 Certificate Expired → Cert Renewal Tree
    DT11: etcd Issues → etcd Health Decision Tree
    DT12: Ingress 502/503 → Backend Debug Tree
    DT13: OOMKilled → Resource Tuning Decision Tree
    DT14: ConfigMap Not Updating → Configuration Debug Tree
    DT15: kubeadm Join Failed → Bootstrap Decision Tree

APPENDIX F: KUBERNETES ARCHITECTURE DIAGRAMS (ASCII ART)
    Full control plane + worker node diagram
    Request flow: kubectl → AuthN → AuthZ → Admission → etcd
    Pod lifecycle: Pending → Running → Succeeded/Failed
    Deployment reconciliation loop
    Service + Endpoints + kube-proxy flow
    Ingress → Service → Pod traffic path
    etcd RAFT consensus diagram

APPENDIX G: AWS INTEGRATION NOTES
    EKS (Elastic Kubernetes Service) vs self-managed kubeadm
    AWS Load Balancer Controller (ALB/NLB) for Services
    EBS CSI Driver for PersistentVolumes
    EFS CSI Driver for ReadWriteMany volumes
    IAM Roles for Service Accounts (IRSA)
    Security Groups for NodePort/Ingress access
    RDS (Relational Database Service) as external database
    ElastiCache (Redis) for Celery task queue

APPENDIX H: EXAM DAY SURVIVAL GUIDE
    PSI Bridge exam environment — what to expect
    Allowed resources: kubernetes.io/docs, kubernetes.io/blog, kubectl --help
    kubectl aliases: k=kubectl, kn=kubectl config set-context --current --namespace
    Time management: 200 questions ÷ 120 minutes = 36 seconds per question
    Flag-and-skip strategy: don't get stuck, come back later
    Common exam pitfalls: wrong context, wrong namespace, YAML indentation errors
    Essential vim/nano shortcuts for editing YAML
    Pre-exam checklist: system test, ID ready, quiet room, water

APPENDIX I: KUBERNETES CHEAT SHEET (ONE-PAGE REFERENCE)
    YAML templates for: Pod, Deployment, Service (all types), Ingress, PVC, ConfigMap, Secret, NetworkPolicy, HPA, RBAC
    kubectl imperative commands: create, expose, set, edit, scale, autoscale, rollout, label, annotate, taint
    JSONPath and custom-columns examples
    Resource abbreviation table: po, deploy, svc, pvc, pv, ns, cm, secret, sa, no

APPENDIX J: FROM NOVICE TO PROFESSIONAL — LEARNING ROADMAP
    🟢 Novice Phase (Weeks 1-4): Containers & Kubernetes fundamentals (F.1-F.5)
        → Storage basics + Workloads & Scheduling (Categories 1 & 3)
        → First deployment of anihpj on a local cluster (S1-S10 lab scenarios)
    
    🟡 Intermediate Phase (Weeks 5-8):
        → Services & Networking deep dive (Category 5)
        → Cluster Architecture + Troubleshooting (Categories 4 & 2)
        → 50+ lab scenarios, HPA, RBAC, Ingress, NetworkPolicies
    
    🔴 Advanced/Professional Phase (Weeks 9-12):
        → High Availability patterns, etcd disaster recovery
        → Operators, CRDs, admission webhooks, Helm/Kustomize hybrid workflows
        → Full-length timed practice exams (200 MCQs under 120 min)
        → ALL 100 lab scenarios under time pressure (exam simulation)
    
    🚀 Beyond CKA (Ongoing):
        → CKAD crossover: multi-container pods, job/cronjob patterns, init containers
        → CKS security: PodSecurityPolicies→PodSecurityStandards, OPA/Gatekeeper, image scanning
        → Production patterns: GitOps (ArgoCD/Flux), progressive delivery, service mesh
        → Multi-cluster: Cluster API, fleet management, hybrid cloud architectures
        → Real-world anihpj: CI/CD pipeline (GitHub Actions → ECR → ArgoCD → EKS)

APPENDIX K: DIFFICULTY INDEX — QUICK REFERENCE
    Q1-Q20    Storage                          🟢 Novice
    Q21-Q44   Troubleshooting (Nodes/Comp)     🟡 Intermediate
    Q45-Q68   Troubleshooting (Monitoring/Logs) 🟡 Intermediate
    Q69-Q80   Troubleshooting (Network)        🔴 Advanced
    Q81-Q98   Workloads (Deploy/ConfigMaps)    🟢 Novice
    Q99-Q110  Workloads (Autoscaling/Schedule)  🔴 Advanced
    Q111-Q130 Cluster (Architecture/kubeadm)   🟡 Intermediate
    Q131-Q150 Cluster (HA/RBAC)                🔴 Advanced
    Q151-Q160 Cluster (Helm/Kustomize/CRDs)    🔴 Advanced
    Q161-Q180 Networking (Pod/Service)         🟡 Intermediate
    Q181-Q200 Networking (Policy/Ingress)      🔴 Advanced

APPENDIX L: BEYOND CKA — CKAD & CKS CROSSOVER REFERENCE
    Topics that appear in CKA but are STRONGER in CKAD:
        • Multi-container pod design patterns (sidecar, adapter, ambassador)
        • Jobs and CronJobs (parallelism, completions, backoffLimit)
        • Init containers (sequential startup dependencies)
        • Pod lifecycle hooks (postStart, preStop)
        • Resource requests/limits tuning for optimal bin packing
    
    Topics that bridge CKA → CKS:
        • PodSecurityStandards (restricted baseline) replacing PSP
        • ImagePolicyWebhook and admission control for image validation
        • etcd encryption at rest (EncryptionConfiguration)
        • Audit policy configuration (who did what and when)
        • TLS/bootstrap token security model
        • OPA/Gatekeeper policy-as-code introduction
    
    Production DevOps Patterns (Real-World):
        • GitOps with ArgoCD/Flux for anihpj deployments
        • Progressive delivery: canary analysis with Argo Rollouts
        • Secret management: External Secrets Operator → AWS Secrets Manager
        • Observability stack: Prometheus + Grafana + Loki + Tempo
        • CI/CD pipeline: GitHub Actions → Docker Build → ECR → ArgoCD → EKS
        • Cost optimization: Karpenter, spot instances, VPA right-sizing
        • Disaster recovery: Velero backups, cross-region etcd snapshots


═══════════════════════════════════════════════════════════════════════
NAVIGATION & INTERACTIVE FEATURES
═══════════════════════════════════════════════════════════════════════

SIDEBAR NAVIGATION (matching cilium-test-prep.html design)
    Collapsible sidebar with category color coding
    Expand All / Collapse All buttons
    Direct anchor links to every question, issue, and scenario
    Part headers with progress indicators

INTERACTIVE ELEMENTS
    MCQ: Click to reveal answer + detailed explanation (hidden by default)
    Troubleshooting: Expandable cause cards (🔴 Most Likely / 🟡 Less Likely / 🟣 New Cluster)
    Lab Scenarios: Copy-to-clipboard button on ALL code blocks
    Decision trees: Card-based flowcharts with Yes/No branching
    Search: Browser-native Ctrl+F compatible with all IDs

VISUAL DESIGN (dark theme, GitHub-inspired)
    5 category accent colors (consistent across all parts)
    Card-based layouts for MCQs, troubleshooting issues, and scenarios
    ASCII art diagrams for architecture, flows, and file trees
    Tables for comparisons, command references, and domain weights
    Code blocks with monospace fonts and syntax highlighting
    Responsive design for desktop and tablet viewing
