                    <div class="info-box warning">
                        <h5>⚠️ Important</h5>
                        <p><strong>demo</strong> profile is for LEARNING only — NOT production! It installs an ingress AND egress gateway with no resource limits, which is convenient for a lab but wasteful at scale. It still does NOT include Kiali/Jaeger/Prometheus/Grafana — that's a separate <code>samples/addons/</code> apply no matter which profile you use. Use <strong>default</strong> for production.</p>
                    </div>
