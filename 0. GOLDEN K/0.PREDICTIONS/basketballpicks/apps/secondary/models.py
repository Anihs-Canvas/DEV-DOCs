"""secondary — SecondaryModule flag registry [bball-01 §1] (copy safepicks
valuebets). Feature-flag rows for EuroLeague / in-play / experimental modules
that may never starve the core loop. STUB owned by the secondary agent.
"""

from django.db import models  # noqa: F401  (import kept so the app is migration-ready)
