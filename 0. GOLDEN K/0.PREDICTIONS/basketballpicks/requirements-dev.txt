-r requirements.txt
# Dev + test tooling. Core-model tests need the ASSEMBLED project (all 12
# apps present) + migrations to run — see apps/core/tests/test_models.py.
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0
ruff>=0.6,<1.0
