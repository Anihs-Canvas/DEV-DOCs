this project is to create a Mptt documentation using the aws_services_full_documentation.html as a semblance

I have created 5 models and their objects
This is the code



# Import necessary modules
from mptt.models import MPTTModel, TreeForeignKey
from mptt.managers import TreeManager
from django.db import models
from django.contrib.auth.models import User

# Hierarchical Skills model - skills can have subcategories
class Skills(MPTTModel):
    name = models.CharField(max_length=200)
    parent = TreeForeignKey('self', on_delete=models.CASCADE, null=True, blank=True, related_name='children')
    
    objects = TreeManager()
    
    class MPTTMeta:
        order_insertion_by = ['name']
    
    def __str__(self):
        return self.name

# Hierarchical Location model - locations can have sub-locations
class Location(MPTTModel):
    name = models.CharField(max_length=200)
    street = models.CharField(max_length=200, blank=True)
    city = models.CharField(max_length=200, blank=True)
    state = models.CharField(max_length=200, blank=True)
    country = models.CharField(max_length=200, blank=True)
    zip = models.CharField(max_length=200, blank=True)
    parent = TreeForeignKey('self', on_delete=models.CASCADE, null=True, blank=True, related_name='children')
    
    objects = TreeManager()
    
    class MPTTMeta:
        order_insertion_by = ['name']
    
    def __str__(self):
        return self.name

# Author remains the same as it doesn't need hierarchy
class Author(models.Model):
    user = models.OneToOneField(User, on_delete=models.CASCADE)
    company = models.CharField(max_length=200)
    designation = models.CharField(max_length=200)
    
    def __str__(self):
        return f"{self.user.first_name} {self.user.last_name}"

# Hierarchical JobPost model - jobs can have sub-jobs or categories
class JobPost(MPTTModel):
    JOB_TYPE_CHOICES = [
        ('Full Time', 'Full Time'),
        ('Part Time', 'Part Time'),
    ]
    
    title = models.CharField(max_length=200)
    description = models.CharField(max_length=200)
    date = models.DateTimeField(auto_now_add=True)
    expiry = models.DateField(null=True)
    salary = models.IntegerField(null=True, blank=True)
    slug = models.SlugField(null=True, max_length=40, unique=True)
    type = models.CharField(max_length=200, null=False, choices=JOB_TYPE_CHOICES, default='Full Time')
    parent = TreeForeignKey('self', on_delete=models.CASCADE, null=True, blank=True, related_name='children')
    
    # Regular foreign keys
    location = models.ForeignKey(Location, on_delete=models.CASCADE, null=True)
    author = models.ForeignKey(Author, on_delete=models.CASCADE, null=True)
    skills = models.ManyToManyField(Skills, related_name='job_posts')
    
    objects = TreeManager()
    
    class MPTTMeta:
        order_insertion_by = ['title']
    
    def __str__(self):
        return self.title



In this chat, i will be asking you questions, and you will be using the models and the objects below to give me examples














Create a complete and  full documentation on **Django MPTT** ( https://django-mptt.readthedocs.io/en/latest/autogenerated.html) based off of the models above into the MPTT_Django.html
Create everthing i need to know
Orgainse it well
Good heading, content structure etc
for each heading, 
give me a 
Full description 
Syntax 
Parameters description (In a non table format) 
Atrributes
Methods (with syntax and parameters description  in a non table format)
Return value (ie what it returns or returns)
Give me 5 very detailed and good examples (make it long) 
print the result for each example 
And show me the queryset output of the print() 
Using the 5 models(Skills, User, Author, Location, JobPost) and the objects created for each of the models above

Create Skills 10 objects
Create User 5 objects 
Create Author 10 objects
Create Location 20 objects
Create JobPost 20 objects

Use this content structure below
* mptt.admin
* mptt.exceptions
* mptt.fields
* mptt.forms
* mptt.managers
* mptt.models
* mptt.querysets
* mptt.utils
* AlreadyRegistered
* register()

use this link as reference : 
https://django-mptt.readthedocs.io/en/latest/index.html and
https://django-mptt.readthedocs.io/en/latest/autogenerated.html



this will be done step by step 
lets start with creating the header, footer, table of contents and sidemenu





next mptt.utils
go to Contentys\mptt.utils.txt
you will find the content there

our format is 
Full description 
Syntax 
Parameters description (In a non table format) 
Atrributes
Methods (with syntax and parameters description  in a non table format)
Return value (ie what it returns or returns)
Give me 5 very detailed and good examples (make it long) 
print the result for each example 
And show me the queryset output of the print() 
Using the 5 models(Skills, User, Author, Location, JobPost) and the objects created for each of the models above




review it, also 
review its side menu



to any section that has these beloow, make sure they exist in the side menu too
Overview
Syntax
Parameters
Attributes
Methods
Examples